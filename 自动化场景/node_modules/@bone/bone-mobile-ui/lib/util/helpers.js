"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getObjectByPropNames = getObjectByPropNames;
exports.addToArray = addToArray;
exports.removeFromArray = removeFromArray;
function getObjectByPropNames(obj) {
  for (var _len = arguments.length, propNames = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    propNames[_key - 1] = arguments[_key];
  }

  return propNames.reduce(function (data, propName) {
    if (obj.hasOwnProperty(propName)) {
      data[propName] = obj[propName];
    }
    return data;
  }, {});
}

// add value, if not duplicated
function addToArray(data, value) {
  if (data.indexOf(value) === -1) {
    data.push(value);
    return true;
  }
  return false;
}

// remove value, if exists
function removeFromArray(data, value) {
  var index = data.indexOf(value);
  if (index !== -1) {
    data.splice(index, 1);
    return true;
  }
  return false;
}